{
  "name": "boombox",
  "private": true,
  "devDependencies": {
    "@types/node": "^10.12.0",
    "lerna": "^3.1.4"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "domain": "lerna run domain",
    "lint": "lerna run lint",
    "deploy": "lerna run deploy"
  },
  "dependencies": {
    "fs": "0.0.1-security",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.3"
  }
}
