// import AWS = require('aws-sdk')

const combineNormalizedTranscriptions = async (event: any, env: { [id: string]: any }) => {
  console.log('success')
}

export const handler = combineNormalizedTranscriptions
