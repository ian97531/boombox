// import AWS = require('aws-sdk')

const updateEpisodeAccessControl = async (event: any, env: { [id: string]: any }) => {
  console.log('success')
}

export const handler = updateEpisodeAccessControl
