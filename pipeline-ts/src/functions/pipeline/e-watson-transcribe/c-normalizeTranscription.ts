// import AWS = require('aws-sdk')

const normalizeSegmentTranscription = async (event: any, env: { [id: string]: any }) => {
  console.log('success')
}

export const handler = normalizeSegmentTranscription
