// import AWS = require('aws-sdk')

const splitEpisodeAudioIntoSegments = async (event: any, env: { [id: string]: any }) => {
  console.log('success')
}

export const handler = splitEpisodeAudioIntoSegments
